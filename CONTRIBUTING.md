# CONTRIBUTING

このリポジトリで開発する際の規約です。変更を加える前に必ず読んでください。

## コーディング規約

- **`any` の使用を禁止します。** 型安全を守るため、`any` は如何なる理由でも使用しないでください。型が分からない場合は `unknown` を使うか、正しい型を調べて明示してください。
- `tsconfig.json` の `strict: true` は維持してください。緩めないでください。

## レビュー

- すべての変更は必ずレビュー（diff の目視確認）を経てからマージしてください。
- 差分が小さく見えても、型の妥当性（特に `any` の混入がないか）を必ず確認してください。

## CI / pre-commit について

- **このリポジトリには CI/CD・pre-commit フックは整備されていません。**
- 型チェック（`npm run typecheck`）は各自のローカル環境で必ず実行してから commit / PR 作成してください。
- lint は必須ツールとして導入されていません。

## コミット前チェックリスト

- [ ] `npm run typecheck` がエラーなしで通ることを確認した
- [ ] `any` を新たに使用していないことを確認した
- [ ] 変更の意図と影響範囲をコミットメッセージに明記した
